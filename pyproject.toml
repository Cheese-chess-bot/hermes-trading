[project]
name = "hermes-trading"
version = "0.1.0"
dependencies = [
    "ccxt",
    "yfinance",
    "pyyaml",
    "httpx",
    "aiofiles",
    "numpy",
    "pandas",
    "rich",
    "supabase",
    "gradio",
]
requires-python = ">=3.11"
